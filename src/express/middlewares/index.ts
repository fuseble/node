import { json, urlencoded } from 'express';
import swagger from './swagger';
import test from './test';
import cors from 'cors';
import pagination from './pagination';

export { json, urlencoded, cors, swagger, test, pagination };
